{
  "name": "aiken-nft-marketplace",
  "version": "1.0.0",
  "description": "Write validators in the `validators` folder, and supporting functions in the `lib` folder using `.ak` as a file extension.",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "tests"
  },
  "scripts": {
    "test": "vitest run"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@meshsdk/core": "^1.8.14",
    "dotenv": "^17.2.3",
    "vitest": "^3.2.4"
  }
}
